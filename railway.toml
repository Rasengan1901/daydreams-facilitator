[build]
builder = "DOCKERFILE"
dockerfilePath = "examples/facilitator-server/Dockerfile"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/supported"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
